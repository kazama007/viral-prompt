<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect all .html requests to .php (301 Permanent)
  RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+(.+?)\.html[\s?] [NC]
  RewriteRule ^ %1.php [R=301,L]

  # DirectoryIndex prefers PHP
  DirectoryIndex index.php index.html
</IfModule>
